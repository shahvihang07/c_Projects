// Functions
/*   Fn prototype
    void printhello();
*/

/*  Fn definition
    void printhello();  {
        printf("Hello");
    }
*/
 
/* Fn Call
    int main() {
    printHello();
    return 0;
    }
*/

// #include<stdio.h>
// void printHello();
// int main() {
//     printHello();
//     printHello();
//     printHello();
//         return 0;
// }
// void printHello() {
//     printf("Hello! \n");
// }

// Properties
// Execution always starts from main
// A function gets called directly or indirectly from main
// There can be multiple functions in a program
// There are 2 type of fn : libary(built in , like printf and scanf) and user defined function.
// No Side Effects: Changes made to parameters inside a function do not change the values in the calling function.  

// Pow function is used only with double , since int can be converted to double easily but not vice versa thus we use %f while printing
// pow fn outputs.


//Recursion Properties
// Anything that can be done with Iteration, can be done with recursion and vice-versa.
// Recursion can sometimes give the most simple solution.
// Base Case is the condition which stops recursion.
// Iteration has infinite loop & Recursion has stack overflow

